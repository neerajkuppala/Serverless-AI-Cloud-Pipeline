version: 1
frontend:
  phases:
    build:
      commands: []
  artifacts:
    # This is the magic line that fixes the 404 error
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []